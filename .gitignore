# Generated by Cargo
/target/

# Cargo.lock for binaries
# Remove this if this is a library project
# Cargo.lock

# Reference Python CLI (cloned locally, not tracked)
azure-cli

# Local tooling config
.mcp.json
resume.sh
